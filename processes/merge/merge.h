#ifndef merge_h
#define merge_h

/*
 * indices for User data values for this process
 */
const int KeepFlynn=0;   // grain which will survive merge
const int RemoveFlynn=1;   // grain which will be removed

#endif
