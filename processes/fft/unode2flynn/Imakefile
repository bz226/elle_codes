#ifdef HasMotif
#ifndef MotifDefines
#ifdef SGIArchitecture
#define XpmTop /usr
#endif
#include <Motif.tmpl>
#endif
#endif

#include "../../../elle.tmpl"

SRCS=  unode2flynn_new5_user.cc
OBJS=  unode2flynn_new5_user.o

EXTRA_LOAD_FLAGS = -L$(TOP)/lib
DEFINES= -DXSYB

THISAPP=unode2flynn_user

.SUFFIXES: .cc .o
.cc .o:
	NormalCplusplusObjectRule()


MakeBatch($(THISAPP),$(BTCHEXT),$(OBJS),$(BINDIRB))

MakeDisplayX($(THISAPP),$(XEXT),$(OBJS),$(BINDIRX))

MakeDisplayWX($(THISAPP),$(WXEXT),$(OBJS),$(BINDIRWX))

CleanMakefile()
